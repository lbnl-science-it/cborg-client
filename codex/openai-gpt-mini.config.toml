# ~/.codex/openai-gpt-mini.config.toml
# OpenAI direct access via ChatGPT Enterprise sign-on (no CBORG_API_KEY needed).
model = "gpt-5.4-mini"
model_reasoning_effort = "medium"
personality = "pragmatic"
